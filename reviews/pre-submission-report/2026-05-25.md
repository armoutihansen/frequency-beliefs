# Pre-Submission Quality Report

**Project:** frequency-beliefs — *The Informativeness of Frequency-Report Scoring Rules*
**Date:** 2026-05-25
**File:** `paper/main.tex`
**Target venue:** general-interest economics journal with a methods section (per `_context/current_status.md`)
**Mode:** sequential 3-audit (compilation + citations + parallel paper-critic / domain-reviewer / code-reviewer)

---

## Integrity Gate: PASS

- **Placeholders:** 0 found (`TODO`, `FIXME`, `XXX`, `TBD`, `[INSERT`, `PLACEHOLDER`, `Lorem ipsum` scan clean)
- **Citation integrity:** all 29 `\cite{}` keys resolve to `references.bib`; 3 unused bib entries (`Friedman1983`, `HuckWeizsaecker2002`, `Schervish1989`) — non-blocking
- **Section completeness:** abstract + 6 sections (Intro, Setup, Scoring Rules, Design Comparison, Discussion, Appendix), all non-empty
- **Broken references:** 0 occurrences of `??` in `main.log`
- **Anonymity gate:** N/A (no double-blind venue selected; author info present in source)

---

## Overall Score: 82/100 — Minor-to-moderate revisions needed

Verdict: **Revise** before submission. No Critical or Major math errors; the paper is methodologically sound and reproduces. Several presentation-level Major issues and two material code-vs-prose mismatches need attention.

---

## Compilation

- **Status:** PASS (26 pages, `main.pdf` regenerated cleanly)
- **Warnings:** 2 overfull hboxes (`02_setup.tex:239–241` ~2.2pt; `07_discussion.tex:332–63` ~1.8pt); no font warnings, no undefined references, no multiply-defined labels
- **Details:** `latexmk -pdf` returns `All targets (main.pdf) are up-to-date`

## Citations

- **Missing keys:** 0
- **Unused keys:** 3 — `Friedman1983`, `HuckWeizsaecker2002`, `Schervish1989` (either cite or remove)
- **Suspect entries:** none flagged via key resolution. DOI verification not run in this pass (sequential mode).
- **Attribution flag:** `Schlag–Tremewan Proposition 1` citation at `03_scoring_rules.tex:183` should be cross-checked against the published JRU 2021 version (domain-reviewer CF1).

## Adversarial Review (paper-critic)

- **Score:** 82/100 — NEEDS REVISION
- **Report:** `reviews/paper-critic/2026-05-25-0822.md`
- **No Critical issues. 5 Major + 7 Minor.**
- **Top Majors:**
  - **M1.** Hardcoded "Section~5" appears 6× but actually points to subsection 5.2 (Hamming/Chebyshev is *inside* the Discussion). Needs a `\label`/`\ref` pass.
  - **M2.** Manhattan one-dimensional threshold root-find depends on a **unimodality** claim (`03_scoring_rules.tex:275`) that is asserted but never proved in the appendix. Only non-trivial fix.
  - **M3.** Abstract collapses the *analytic* crossover (in m) with the *empirical* crossover (in α); intro is more careful — bring abstract in line.
  - **M4.** Systematic UK/US spelling inconsistency (mostly UK: "favourable", "behaviour", "minimises"; pockets of US: "centerpiece", "minimize" in §3, "behavior" in appendix, "optimizing").
  - **M5.** Abstract phrase *"per-coordinate expected loss"* misdescribes frequency-guessing (body explicitly notes a log transformation is needed). One-line abstract rewrite fixes both M3 and M5.

## Domain Review (math/derivations)

- **Verdict:** PASS — **no Critical or Major findings**
- **Report:** `reviews/domain-reviewer/2026-05-25-0822.md`
- All theorems and proofs independently re-derived: Propositions 1–3, Lemma 1, Corollary 1 hold; squared-distance integrality bound, frequency-guessing multiplicative form (p_j=0 boundary), Manhattan threshold derivation (Δℓ_i = 2F_i − 1), single-transfer transport, and the n=3, k=5 Hamming counterexample all check.
- **11 Minor exposition tightenings**, none altering validity. Notably:
  - **BL1.** Abstract's "sharp semi-analytical bounds" for Manhattan blurs the within-rule distinction between exact-1D coordinate bounds and grid-tolerance mean bounds.
  - **BL3.** §3.4 "Manhattan reduces to a 1D threshold computation" reads as blanket; only *coordinate* bounds are 1D root-finds — mean bounds are 1D optimisations implemented on a grid.
  - **D1, D4.** Two appendix proofs need one-line annotations (nonnegativity of zero-report coordinates; continuity/strict monotonicity of F_i in p for the inverse-CDF step).
  - **CT4.** Scripts still label rules `discrete` / `quadratic` / `manhattan`; paper standardised on `frequency-guessing` / `squared-distance` / `Manhattan`. Worth aligning.

## Code Review (reproducibility)

- **Report:** `reviews/code-reviewer/2026-05-25-0822.md`
- `verify_regions.py` runs cleanly under `uv run` — 13/13 finite checks pass.
- `consistency_check.py` reproduces every numerical claim in §4 and Table 1 from `outputs/design_exercise/`.
- Simulation seed `20260508` is fixed; tie rates are 0; payment probability is emitted only for the discrete-metric rule (correct per CLAUDE.md).
- **Two material issues:**
  - **C1.** README points to `outputs/simulation_design/` but the manuscript regret table can only be produced from `outputs/design_exercise/` (legacy `simulation_design/rule_wins.csv` lacks the `mean_regret` column and uses the older grid-based Manhattan algorithm).
  - **C2.** `06_design_comparison.tex:56` claims squared-distance "gives the narrowest maximum-coordinate interval on a majority of draws for every α ≤ 3" — data give 0.907 / 0.802 / 0.630 for α ∈ {0.1, 0.3, 1} but **0.442 at α=3**, which is *not* a majority. Tighten to "α ≤ 1".
- **C3 (Minor).** Paper rounds the whole-grid mean coordinate widths to "0.102, 0.104, 0.102", but Discrete actually rounds to **0.103**, not 0.102.
- **Context note.** `CLAUDE.md` still lists four headline rules and ascribes the four-rule horse race; `_context/current_status.md` (2026-05-22) explicitly supersedes this — the project is three-rule (Hamming excluded per ADR-0001 fourth amendment). The discrepancy is documentation-only, but `CLAUDE.md` should be updated post-submission.

---

## Consolidated P0/P1/P2 Fix List

### P0 — block submission
*(none — compilation passes, no fabricated citations, no math errors, no anonymity issues)*

### P1 — must fix before submission

| # | Source | Location | Issue |
|---|---|---|---|
| 1 | code-reviewer C2 | `paper/sections/06_design_comparison.tex:56` | "every α ≤ 3" overclaim: α=3 win-share is 0.442 (not majority). Tighten to "α ≤ 1". |
| 2 | paper-critic M2 | `paper/sections/03_scoring_rules.tex:275`; appendix | Manhattan threshold root-find relies on **unimodality** of `Δℓ_i(t; p_i) = 2F_i(t; n, p_i) − 1` in t — assertion is not justified. Either prove it (one paragraph in the appendix, since F_i is the binomial CDF in t at fixed p_i — strict monotonicity in t suffices), or restate the result conditionally. |
| 3 | paper-critic M1 | All §5 cross-references | "Section~5" appears 6× but should point to subsection 5.2 (Hamming/Chebyshev). Mechanical `\label`/`\ref` pass. |
| 4 | paper-critic M3+M5 | `paper/main.tex` abstract (lines 36–42) | Abstract collapses analytic m-crossover with empirical α-crossover and misdescribes frequency-guessing as "per-coordinate expected loss". One-line rewrite resolves both. |
| 5 | paper-critic M4 | All `.tex` | UK/US spelling normalization — pick one (paper is dominantly UK). |
| 6 | code-reviewer C1 | `README.md` (and project layout) | README points to stale `outputs/simulation_design/`; manuscript uses `outputs/design_exercise/`. Update README or rename directory. |
| 7 | code-reviewer C3 | `paper/sections/06_design_comparison.tex` (whole-grid means) | "0.102, 0.104, 0.102" should be **0.102, 0.104, 0.103** (Discrete rounds to 0.103). |

### P2 — should consider

| # | Source | Location | Issue |
|---|---|---|---|
| 8 | domain-reviewer BL1 | `paper/main.tex` abstract | "Sharp semi-analytical bounds" for Manhattan blurs coordinate-vs-mean distinction. |
| 9 | domain-reviewer BL3 | `paper/sections/03_scoring_rules.tex:266-282` | §3.4 reads as if all Manhattan bounds are 1D root-finds; only coordinate bounds are — mean bounds are grid-implemented optimisations. |
| 10 | domain-reviewer D1 | `paper/sections/08_appendix.tex:96-107` | One-line annotation on `Σ_{v ≠ i, v zero-report} p_v ≤ 1 − p_i` step. |
| 11 | domain-reviewer D4 | `paper/sections/08_appendix.tex:281` | Manhattan inversion needs explicit continuity + strict monotonicity of F_i in p (used by `brentq`). |
| 12 | domain-reviewer CF1 | `paper/sections/03_scoring_rules.tex:183` | Verify Schlag–Tremewan "Proposition 1" attribution against published JRU 2021. |
| 13 | domain-reviewer CT4 | `scripts/*.py` | Rename script rule labels (`discrete`/`quadratic`/`manhattan`) to match paper terminology (`frequency-guessing`/`squared-distance`/`Manhattan`) — coordination, not correctness. |
| 14 | paper-critic minors | various | `\DeltaK` macro inconsistency in intro; "say something clean" / "this binary case is clean" hedges; 2 sub-2pt overfull hboxes; comparison corollary breaking 3.3/3.4 flow; opaque `SchlagTremewanSimple` bibkey; table captions missing per-cell `N=5000` disclosure; §4 mechanism claim slightly overconfident. |
| 15 | citations | `references.bib` | Drop `Friedman1983`, `HuckWeizsaecker2002`, `Schervish1989` if not citing, or add them. |
| 16 | CLAUDE.md | repo root | Stale (still says "four headline rules") — superseded by `_context/current_status.md`. Update post-submission so future audits don't trip on it. |

---

## Research Quality Score

(no rubric file resolved in this environment; using the verdict band from `quality-scoring.md`)

- Methodological rigor: high (domain-review: no Critical/Major; math independently re-derived)
- Reproducibility: high (`verify_regions.py` clean, simulation seed fixed, consistency check reproduces)
- Exposition: needs polish (M1–M5 are presentation, not substance)
- Positioning / contribution: clear; honest "no universal winner" framing supported by Corollary 1 and Table 1

**Weighted aggregate (informal):** ≈ 4.2 / 5.0 — *minor revisions*

---

## Recommendation

**Revise** — the paper is mathematically sound and reproduces. Before submitting, action the 7 P1 items (most are 1–2 line fixes). The only non-mechanical P1 is **M2** (Manhattan unimodality), which is a short appendix paragraph since `F_i(t; n, p_i)` is the binomial CDF in t at fixed p_i (strict monotonicity in t gives unique threshold). Estimated revision time: half a day for P1, another half day if you also clear P2.

**Top 3 to fix first:**

1. **P1 #1** — tighten the "α ≤ 3" claim in §4 to "α ≤ 1" (a numerical-overstatement liability).
2. **P1 #2** — justify Manhattan unimodality (otherwise the appendix and `brentq` use are formally unsupported).
3. **P1 #4** — abstract rewrite resolving M3+M5 (the abstract is the first thing a referee reads).

---

## Sub-Agent Reports

| Agent | Report path |
|---|---|
| paper-critic | `reviews/paper-critic/2026-05-25-0822.md` (+ `.findings.json`) |
| domain-reviewer | `reviews/domain-reviewer/2026-05-25-0822.md` |
| code-reviewer | `reviews/code-reviewer/2026-05-25-0822.md` |
