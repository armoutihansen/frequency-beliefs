# Code Reviewer: Second Audit (Re-audit)

Date: 2026-05-25 (R2)
Scope: re-audit of fixes from 2026-05-25 review; commands run with `uv run python ...`.

## 1. File reviewed

- /Users/jesper/repos/frequency-beliefs/scripts/verify_regions.py
- /Users/jesper/repos/frequency-beliefs/scripts/design_efficiency.py
- /Users/jesper/repos/frequency-beliefs/scripts/consistency_check.py
- /Users/jesper/repos/frequency-beliefs/paper/sections/06_design_comparison.tex
- /Users/jesper/repos/frequency-beliefs/paper/sections/08_appendix.tex
- /Users/jesper/repos/frequency-beliefs/README.md
- /Users/jesper/repos/frequency-beliefs/outputs/design_exercise/{rule_comparison,latent_aggregate,fixed_reports}.csv

## 2. Purpose of code

- `verify_regions.py`: finite computational checks of rule characterizations and region claims (13 check families).
- `design_efficiency.py`: latent-belief simulation comparing three rules (discrete metric, quadratic, Manhattan) over an (n,k,alpha) grid; produces three CSVs in `outputs/design_exercise/`.
- `consistency_check.py`: one-off computational check that the manuscript's design-comparison numbers reproduce from the CSVs and that Corollary 1 holds for the closed-form rules.

## 3. Correctness concerns

- None blocking. All four targeted fixes (C1 README, C2 alpha threshold, C3 rounded triple, docstring) re-checked and verified below.
- One minor inconsistency persists: `scripts/design_efficiency.py` line 851 says outputs/simulation_design "is not overwritten by this revised script," but the docstring at top calls those labels archival pre-2026-05-22. Wording is internally consistent with README's archival note.

## 4. Reproducibility concerns

- `uv run python scripts/verify_regions.py`: 13/13 check families pass. Worked-examples markdown rendered. No failures.
- `uv run python scripts/consistency_check.py`: all numbers reproduce from CSV outputs and match the manuscript tables.
- `uv run python -m py_compile scripts/design_efficiency.py scripts/verify_regions.py scripts/consistency_check.py`: exit 0.
- Random seed for the simulation is fixed at 20260508 (`design_efficiency.py:848`).

## 5. Consistency with paper

Spot checks against `outputs/design_exercise/`:

- **C1 README fix**: README line 43 points to `outputs/design_exercise/` with an archival note about `outputs/simulation_design/`. Both directories exist; the legacy directory contains the older four-rule CSVs (with `rule_wins.csv` rather than `rule_comparison.csv`). README internally consistent.
- **C2 alpha threshold fix**: Paper line 56 ("every alpha <= 1") is supported. Aggregated coord_max win shares for Quadratic: alpha=0.1 → 0.907, 0.3 → 0.802, 1.0 → 0.630, 3.0 → 0.442, 10.0 → 0.260. So Quadratic is a strict majority at alpha in {0.1, 0.3, 1} but not at 3 or 10. The tightened claim is correct.
- **C3 rounded triple fix**: Paper line 29 ("0.103, 0.104, 0.102") reproduces from `latent_aggregate.csv`: Discrete 0.1025, Quadratic 0.1042, Manhattan 0.1021. Rounding to three decimals gives 0.103, 0.104, 0.102.
- **Table tab:regime-wins (06 lines 41-45)**: matches CSV.
- **Table tab:regime-regret (06 lines 77-81)**: matches CSV (Discrete 0.032 at alpha=0.1 down to 0.000 at alpha>=3; Quadratic 0.002 to 0.023; Manhattan 0.007-0.014).
- **Table tab:design-nk (08 lines 354-363)**: every cell matches CSV aggregation by k and by n.
- **Robustness paragraph (08 line 373)**: linear-mean win shares 0.527/0.378/0.096 (evenly spaced) and 0.557/0.345/0.099 (extreme) round to the stated 0.53/0.38/0.10 and 0.56/0.34/0.10.
- **Payment-probability claim (06 lines 96-100)**: at n=50, k=10, alpha in {1,3,10} the Discrete-metric payment_probability_mean is 1.1e-5, 1.1e-6, 5.7e-7 -- "essentially zero" is accurate.

## 6. Mathematical claims covered by code

- Lemma 1 (single-transfer characterization): finite enumeration check in `verify_regions.py`.
- Proposition (quadratic): projection optima, halfspace and one-count transfer agreement, and closed-form coordinate intervals checked against LPs.
- Proposition (frequency-guessing): mode characterization and interval formula checked on grids.
- Proposition (Manhattan): separable binomial objective, delta formula, exchange/CDF inequalities, binary median intervals checked.
- Corollary 1 (closed-form average widths): explicitly recomputed in `consistency_check.py` for every (n,k) in {2,3,5,10} x {2,5,10,20}; pass.
- Direct-monetary risk reversals: 2 enumeration checks pass.

## 7. Mathematical claims not covered by code

- Lemma 2 (single-equation characterization of Manhattan coordinate bounds): proved in Appendix A but not verified computationally (e.g., no script confirming that the root-finder converges to the same value as a fine threshold sweep on a battery of cases). Not blocking, but a small numeric sanity check would tighten reproducibility.
- Hamming-distance characterization is checked at the optima level but the paper relegates Hamming to Section 'other rules'; this is consistent.
- The unimodality argument in Lemma 2 (phi(c) decreasing) is not separately tested numerically.

## 8. Recommended changes

- (Optional) Add a tiny test inside `verify_regions.py` that compares the Manhattan threshold root-find against the simplex-grid diagnostic on the existing worked examples to keep the bound implementation under continuous check.
- (Optional, low priority) Rename internal `RULES` labels from {"discrete","quadratic","manhattan"} to the paper terminology and re-run, so CSVs and prose share vocabulary. The new docstring (lines 11-26) documents the mapping clearly and explicitly warns against renaming without re-running; if renaming is deferred, no change is needed.
- (Optional) In `design_efficiency.py` line 851 the in-code comment about `outputs/simulation_design/` could be aligned with the README phrasing ("archival reference") for full consistency.

## 9. Rule-label docstring verification

- `RULES = ("discrete", "quadratic", "manhattan")` (line 66).
- `RULE_LABELS` map (lines 67-71) emits "Discrete metric", "Quadratic distance", "Manhattan distance" -- matches CSV column "rule" values.
- `outputs/design_exercise/rule_comparison.csv` header row contains "Discrete metric" / "Quadratic distance" / "Manhattan distance"; the paper uses "frequency-guessing" / "squared-distance" / "Manhattan(-distance)".
- The docstring mapping is therefore accurate and useful.

## 10. Commands to run

```bash
uv run python scripts/verify_regions.py
uv run python scripts/consistency_check.py
uv run python -m py_compile scripts/design_efficiency.py scripts/verify_regions.py scripts/consistency_check.py
```

(Did not re-run `uv run python scripts/design_efficiency.py --final --draws 5000` to avoid overwriting `outputs/design_exercise/`.)

## 11. Whether outputs should be regenerated

- No. `outputs/design_exercise/` is consistent with all manuscript numbers verified above. Do not regenerate.

## Summary of fixes verified

| Item | Status |
|---|---|
| C1 README points to design_exercise/ with archival note | verified |
| C2 alpha<=1 (not <=3) in 06 line 56 | verified |
| C3 rounded triple 0.103/0.104/0.102 in 06 line 29 | verified |
| verify_regions.py still 13/13 | verified (13 check families, 0 fails) |
| consistency_check.py reproduces all manuscript table numbers | verified |
| Rule-label docstring accurate | verified |
| Tables in 06 and 08 (Table 1, regret, design-nk) | spot-checked, match |
| Payment-probability claim n=50,k=10 ~ 0 | verified |
| Robustness paragraph extreme vs evenly spaced | verified |

