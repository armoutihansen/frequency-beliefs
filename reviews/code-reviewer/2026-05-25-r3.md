# Code Reviewer — Round 3 (post-C5 robustness)

Date: 2026-05-25
Reviewer: code-reviewer
Verdict: **PASS**

## 1. File reviewed
- `/Users/jesper/repos/frequency-beliefs/scripts/design_efficiency.py`
- `/Users/jesper/repos/frequency-beliefs/scripts/consistency_check.py`
- `/Users/jesper/repos/frequency-beliefs/scripts/verify_regions.py`
- `/Users/jesper/repos/frequency-beliefs/outputs/design_exercise/robustness/{rule_comparison,latent_aggregate}.csv`
- `/Users/jesper/repos/frequency-beliefs/paper/sections/08_appendix.tex` (lines 370–402)
- `/Users/jesper/repos/frequency-beliefs/paper/sections/06_design_comparison.tex` (§5.3 / line 57)

## 2. Purpose of code
New asymmetric-Dirichlet robustness companion to the symmetric horse race: three pre-registered concentration vectors at `n=20, k=5`, written to a separate subdirectory so the main outputs are untouched.

## 3. Reproducibility check
- `uv run python scripts/consistency_check.py` — all three checks PASS:
  - Check 2 (Corollary 1): PASS across `(n,k) ∈ {2,5,10,20}×{2,3,5,10}`.
  - Check 1 (main grid prose): all alpha rows reproduce; overall mean coord widths 0.1025 / 0.1042 / 0.1021 match paper's 0.103 / 0.104 / 0.102.
  - Check 3 (asymmetric robustness): all 18 win-share entries match `tab:regime-wins-asymmetric` to 0.01 tolerance — `(0.06,0.60,0.33)`, `(0.04,0.72,0.24)`, `(0.03,0.76,0.21)`, `(0.04,0.82,0.14)`, `(0.07,0.67,0.26)`, `(0.03,0.86,0.11)`.
- `uv run python scripts/verify_regions.py` — 13/13 PASS unchanged.
- `uv run python scripts/design_efficiency.py --robustness-only --draws 5000` reproduces the committed CSVs bit-for-bit (`diff` on both files returned empty against `/tmp/robustness_repro/robustness/`). Seed `20260508` propagates correctly.

## 4. Code-vs-prose drift checks
Verified the additional prose claims in §A's robustness paragraph (08_appendix.tex lines 399–401) against the CSV:
- "squared-distance's mean regret is ≤ 0.003 in every asymmetric cell" → CSV: 0.0020, 0.0011, 0.0016. OK.
- "frequency-guessing's [regret] reaches 0.012 (two-mode)" → CSV: 0.0121. OK.
- "Manhattan's stays at 0.002–0.004" → CSV: 0.0027, 0.0037, 0.0024. OK.

The §5.3 pointer (06_design_comparison.tex line 57) and §A subsection caption are descriptive only; no new quantitative claims beyond the table and the regret prose.

## 5. Consistency with paper
All numerical claims match the source CSVs. Internal labels (`discrete`/`quadratic`/`manhattan`) vs paper terminology (Guessing/Squared/Manhattan) are bridged correctly in the consistency check.

## 6. Mathematical claims covered by code
- Corollary 1 closed-form widths (W0 constant for discrete, WQ(m) for quadratic, W_Q(1)<W0<W_Q(k)).
- Sharp Manhattan coordinate bounds via threshold root-find; threshold-grid Manhattan mean bounds.
- Win-share/regret design comparison under both symmetric and (new) asymmetric Dirichlet draws.

## 7. Mathematical claims not covered by code
- No new uncovered claims introduced in this round. (Hamming still excluded from the simulation by design per ADR-0001 second amendment; the script's docstring states this clearly at line 790.)

## 8. Recommended changes
Minor / optional, none blocking:
1. The new `run_robustness_simulation()` uses `update_aggregate()` (line 665), which appends to `entry["payment"]` only for the discrete rule. Then at line 700–701 it computes `payment_probability_mean/median` only if `vals["payment"]` is truthy. Behaviour is identical to the symmetric path — but a brief docstring note that payment metrics are populated only for the discrete rule would prevent future confusion when reading the robustness aggregate CSV.
2. `ROBUSTNESS_CELLS` is a module-level constant with hard-coded `n=20, k=5`. The script's `--robustness-only` flag takes `--draws` but not the cells. Acceptable for a frozen design, but worth a one-line comment that these are pre-registered. (The appendix prose does state "Three asymmetric concentration vectors at n=20, k=5".)
3. The four-rule discipline: the robustness output, like the main horse race, still reports `payment_probability_mean` only for the discrete rule. This matches CLAUDE.md guidance and is not used as a cross-rule comparison metric. No change needed.

## 9. Commands to run
```bash
uv run python scripts/consistency_check.py
uv run python scripts/verify_regions.py
uv run python -m py_compile scripts/design_efficiency.py scripts/verify_regions.py scripts/consistency_check.py
# Reproducibility spot-check (writes to a tempdir, not outputs/):
uv run python scripts/design_efficiency.py --robustness-only --draws 5000 --output-dir /tmp/robustness_repro
diff /tmp/robustness_repro/robustness/rule_comparison.csv outputs/design_exercise/robustness/rule_comparison.csv
```

## 10. Whether outputs should be regenerated
**No.** The committed `outputs/design_exercise/robustness/{rule_comparison,latent_aggregate}.csv` reproduce exactly under the documented seed and draws. Per CLAUDE.md, final outputs should not be overwritten.

## Round-3 verdict
**PASS.** No code-vs-prose drift, no reproducibility issue, no four-rule-discipline violation.
